function qkSort(arr){
    var pI = arr.length - 1
    var left = []
    var right = []
    for (i=0, i < arr.length, i++){
        if (arr[i]<arr){

        } 
    }
}

arr = [5, 4, 1, 0, 7, 2, 6, 3]

console.log(qkSort(arr))
